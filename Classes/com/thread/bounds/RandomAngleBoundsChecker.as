package com.thread.bounds {	import com.thread.bounds.AbstractBoundsChecker;	import com.thread.bounds.IBoundsChecker;	import com.thread.constant.ThreadConstants;	import com.thread.vo.IMotionable;	import flash.geom.Point;	/**	 * @author plemarquand	 */	public class RandomAngleBoundsChecker extends AbstractBoundsChecker implements IBoundsChecker 	{		public function RandomAngleBoundsChecker(target : IMotionable)		{			super( target );		}				override public function checkBounds(x : Number, y : Number) : Point		{			if((x > ThreadConstants.MANAGER_WIDTH || x < 0) || (y > ThreadConstants.MANAGER_HEIGHT || y < 0))			{				_target.angle += 45 + Math.random() * 90;				return resetPosition();			}						savePosition(x, y);			return new Point(x, y);		}	}}